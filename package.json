{
  "name": "express-supertest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ext .js --ignore-path .gitignore . -f codeframe --fix",
    "test": "npm-run-all test:*",
    "test:docker-pull": "docker pull amazon/dynamodb-local",
    "test:docker-run": "docker run -d --name dynamodb -p 8000:8000 amazon/dynamodb-local",
    "test:env": "node createTable.js",
    "test:exec": "jest",
    "test:docker-stop": "docker container stop `docker container ls -q`",
    "test:docker-rm": "docker container rm `docker container ls -a -q`"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajv": "^6.10.0",
    "aws-sdk": "^2.470.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-winston": "^3.2.0",
    "helmet": "^3.18.0",
    "jsonwebtoken": "^8.5.1",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^4.3.0",
    "eslint-plugin-jest": "^22.6.4",
    "eslint-plugin-prettier": "^3.1.0",
    "jest": "^24.8.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.17.1",
    "supertest": "^4.0.2"
  }
}
